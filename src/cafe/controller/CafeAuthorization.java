package cafe.controller;

import org.springframework.context.annotation.Configuration;
import org.springframework.http.HttpMethod;
//import org.springframework.security.config.annotation.web.builders.HttpSecurity;
//import org.springframework.security.config.annotation.web.configuration.WebSecurityConfigurerAdapter;

//@Configuration
//public class CafeAuthorization extends WebSecurityConfigurerAdapter {
public class CafeAuthorization {

	// @Override
	// protected void configure(HttpSecurity http) throws Exception {
	// http.authorizeRequests().antMatchers(HttpMethod.POST).hasRole("USER").and().authorizeRequests()
	// .antMatchers("/currencyNames").authenticated().and().authorizeRequests().anyRequest().permitAll().and()
	// .httpBasic().and().csrf().disable();
	// }
}
